#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;

    vector<int> time;

    for (int i = 0; i < n; i++) {
        int f, s, t;
        cin >> f >> s >> t;

        time.push_back(f);
    }

    return 0;
}